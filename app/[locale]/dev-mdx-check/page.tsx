import PipelineCheck from "@/content/dev/pipeline-check.mdx";

// Temporary MDX-pipeline verification route — replaced by the real lesson page in phase 3.
export default function DevMdxCheckPage() {
  return (
    <main className="mx-auto w-full max-w-3xl flex-1 px-4 py-10">
      <PipelineCheck />
    </main>
  );
}
